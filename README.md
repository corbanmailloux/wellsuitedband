# Well Suited's Band Website

[wellsuitedband.com](https://wellsuitedband.com)

### Development Setup

- Install dependencies: `npm install` or `make install`
- Run dev server: `npm run dev` or `make dev`
- Build for production: `npm run build`
